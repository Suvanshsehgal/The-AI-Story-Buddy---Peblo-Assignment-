import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Purple (logo, headings)
  static const Color primaryPurple = Color(0xFF6B21E8);
  static const Color primaryPurpleLight = Color(0xFF9B59F5);

  // Yellow / Gold (CTA buttons)
  static const Color accentYellow = Color(0xFFF5C518);
  static const Color accentYellowDark = Color(0xFFE0A800);

  // Teal / Cyan (top section background)
  static const Color tealBackground = Color(0xFF5CE1E6);
  static const Color tealLight = Color(0xFFB2F0F2);

  // Pink / Coral (middle sections)
  static const Color pinkSection = Color(0xFFF26B8A);
  static const Color pinkLight = Color(0xFFFDE8EE);

  // Orange / Amber (Joy section)
  static const Color amberSection = Color(0xFFF4A623);

  // Pastel card backgrounds
  static const Color cardCream = Color(0xFFFFF9F0);
  static const Color cardLavender = Color(0xFFF0EDFF);
  static const Color cardMint = Color(0xFFEDFFF5);
  static const Color cardBlush = Color(0xFFFFEDF2);
  static const Color cardSky = Color(0xFFEDF6FF);

  // Dark navy (footer)
  static const Color darkNavy = Color(0xFF1A1040);

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color offWhite = Color(0xFFFAF8FF);
  static const Color textDark = Color(0xFF1A1040);
  static const Color textMedium = Color(0xFF4A4A6A);
  static const Color textLight = Color(0xFF9090B0);

  // Quiz specific
  static const Color optionDefault = Color(0xFFFFFFFF);
  static const Color optionCorrect = Color(0xFF4CAF50);
  static const Color optionWrong = Color(0xFFFF5252);
  static const Color optionBorderDefault = Color(0xFFDDD8F5);
  static const Color optionBorderSelected = Color(0xFF6B21E8);
}